import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    height: '400px',
    marginTop: '20px',
    padding: '20px 10px',
    backgroundColor: '#fff'
  }
});

export default useStyles;